'use client'; import {useState} from 'react'; import {PageLayout,Breadcrumbs} from '@/components/page-layout'; import {site} from '@/data/products'
export default function ContactPage(){const [sent,setSent]=useState(false);return <PageLayout><Breadcrumbs items={[{label:'Contact'}]}/><section className="page-hero"><p className="eyebrow">PC Shack / Find us</p><h1>Come and see us <em>in Hull.</em></h1><p>Questions about repairs, hardware or a custom build? Send a message and our team will get back to you.</p></section><div className="contact-layout"><address><h2>PC Shack</h2>{site.address.map(a=><span key={a}>{a}</span>)}<a href={`tel:${site.phone}`}>{site.displayPhone}</a><a href={`mailto:${site.email}`}>{site.email}</a><a href={`mailto:${site.customerEmail}`}>{site.customerEmail}</a><div className="map-placeholder">HU3 4TT<br/><small>Map location to be configured</small></div></address><form className="contact-form" onSubmit={e=>{e.preventDefault();setSent(true)}}>{sent?<div className="success-state"><h2>Message received.</h2><p>Thanks — we&apos;ll be in touch using the details you provided.</p></div>:<><h2>Contact PC Shack</h2><label>Name<input required name="name"/></label><label>Email<input required type="email" name="email"/></label><label>Phone<input name="phone"/></label><label>Subject<input required name="subject"/></label><label>Message<textarea required rows={6} name="message"/></label><button className="button button-primary">Send Message ↗</button></>}</form></div></PageLayout>}

import Link from 'next/link'; import {PageLayout,Breadcrumbs} from '@/components/page-layout'; import {services} from '@/data/products'; import {ArrowUpRight} from 'lucide-react'
export default function ServicesPage(){return <PageLayout><Breadcrumbs items={[{label:'Services'}]}/><section className="page-hero"><p className="eyebrow">PC Shack / What we do</p><h1>Practical help. <em>Properly done.</em></h1><p>Support for home users, creatives and growing businesses across Hull and East Yorkshire.</p></section><section className="service-directory">{services.map((s,i)=><Link href={s.href} className="service-feature" key={s.title}><span>0{i+1}</span><div><h2>{s.title}</h2><p>{s.description}</p></div><ArrowUpRight/></Link>)}</section></PageLayout>}

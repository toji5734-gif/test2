'use client'; import {notFound} from 'next/navigation'; import Link from 'next/link'; import {useState} from 'react'; import {PageLayout,Breadcrumbs} from '@/components/page-layout'; import {getProduct,products,formatCurrency} from '@/data/products'; import {useCart} from '@/components/cart-provider'
export default function ProductPage({params}:{params:{slug:string}}){const p=getProduct(params.slug); if(!p) return notFound(); const [qty,setQty]=useState(1);const {add}=useCart();return <PageLayout><Breadcrumbs items={[{label:'Shop',href:'/shop'},{label:p.category,href:'/shop'},{label:p.name}]}/><div className="product-detail"><div><img className="product-detail-image" src={p.image} alt="Development image placeholder"/><span className="placeholder-label">DEVELOPMENT PLACEHOLDER — NOT A REAL PC SHACK PRODUCT</span></div><div className="product-detail-copy"><small>{p.category}</small><h1>{p.name}</h1><p>{p.description}</p><strong className="price-large">{formatCurrency(p.price)}</strong><p className="stock">Test stock · {p.stock} available</p><div className="quantity"><button onClick={()=>setQty(Math.max(1,qty-1))}>−</button><b>{qty}</b><button onClick={()=>setQty(qty+1)}>+</button></div><button className="button button-primary" onClick={()=>{for(let i=0;i<qty;i++)add(p.id)}}>Add to Cart</button><dl className="specs">{Object.entries(p.specifications??{}).map(([k,v])=><div key={k}><dt>{k}</dt><dd>{v}</dd></div>)}<div><dt>SKU</dt><dd>{p.sku??'To be assigned'}</dd></div></dl></div></div><section className="related"><p className="eyebrow">More from the catalogue</p><h2>Related <em>items</em></h2><Link href="/shop" className="text-link">Back to Shop ↗</Link></section></PageLayout>}

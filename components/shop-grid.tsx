'use client'; import Link from 'next/link'; import {ShoppingBag} from 'lucide-react'; import {Product,formatCurrency} from '@/data/products'; import {useCart} from './cart-provider'
export function ShopGrid({products}:{products:Product[]}){const {add}=useCart();return <div className="product-grid">{products.map(p=><article className="product-card" key={p.id}><Link href={`/shop/${p.slug}`}><img src={p.image} alt="Image placeholder"/><span className="placeholder-label">DEVELOPMENT PLACEHOLDER — NOT A REAL PC SHACK PRODUCT</span><div className="product-info"><small>{p.category}</small><h2>{p.name}</h2><p>{p.description}</p><strong>{formatCurrency(p.price)}</strong><span className="stock">Test stock · {p.stock} available</span></div></Link><button className="button button-primary" onClick={()=>add(p.id)}><ShoppingBag size={16}/> Add to Cart</button></article>)}</div>}
